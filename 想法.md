# 富文本编辑器
## 核心功能
- 导入导出word，保真度高。
- word可视化插入输入框等。比如我插入一个债权名称的输入框，他有债权名称、输入限制、默认值等属性。又比如我可以插入一个付息日，他有付息日、默认值、时间范围等属性值。
- word还可以插入可变段落。比如我有一个段落，可变段落不同场景（比如A、B、C）值，显示不同的内容。在内容生成是后端会返回是什么场景。
- word可以保存为模板。
- word模板进入内容生成部分。左侧预览预览模式，自定义项有一个高亮显示（只是页面显示，实际没有）。点击左侧自定义项，右侧可以读取属性，为不同的输入框等。编辑真实值等内容。

## 技术选型
- word尽量高保真，我之前调研过docx-editor
- 尽量不收费的开源、支持二开。
- vue或者react技术栈。

## demo
monorepo架构，包括核心编辑器和增删改查模板等示例。